# VPN

alias vpn="sudo openconnect --protocol=gp --usergroup=gateway -u ${USER} bluevpn-external-netopswest.charter.com"

# AWS

export AWS_PROFILE=charterdev
export AWS_REGION=us-west-2

# Kops

export KOPS_STATE_STORE=s3://state.kubernetes.dev-charter.net
export KOPS_RUN_OBSOLETE_VERSION="true"
